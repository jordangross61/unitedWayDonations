<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.cs2340.buzzTracker">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controllers.WelcomeActivity"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controllers.LoginActivity"
            android:label="Buzz Tracker Login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.DashboardActivity"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.RegisterLocation"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.DashboardActivityEmployee"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.RegistrationActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA3uLpITJlvyWW7k1S0p8eVIgzF4LP2pZg" />

        <activity
            android:name=".controllers.MapsActivity"
            android:label="@string/title_activity_maps"></activity>

        <activity
            android:name=".controllers.DataItemDetailFragment"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.LocationsActivity"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.DataItemListActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.LocationDetailActivity"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.LocationListActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.AddCategoryActivity"
            android:label="Buzz Tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.AddItemActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.AddSizeItemActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.SearchActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.SearchResultsActivity"
            android:label="Buzz Tracker Registration"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>